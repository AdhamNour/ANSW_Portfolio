import { FunctionComponent } from 'react';
import './index.scss';
interface HomeProps {
    
}
 
const Home: FunctionComponent<HomeProps> = (props) => {
    return ( <>Hello</> );
}
 
export default Home;